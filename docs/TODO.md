# 앞으로 해야 할 것

작업을 멈춘 지점과 이어서 할 일. 다음 세션이 여기부터 읽으면 된다.
끝난 항목은 지우지 말고 완료 표시만 남긴다 — 왜 그렇게 정했는지가 같이 사라진다.

---

## 바로 다음

### [완료 2026-08-15] 1. OpenAI / Google 모델 단가 채우기

`webapp/providers.js` 의 `RECOMMENDED.openai` 와 `.google` 을 공식 요금 페이지
조회로 채웠다. 제공자별 최고 성능 3 + 가성비 2, tier 포함.

- OpenAI (출처: developers.openai.com/api/docs/pricing + /models):
  top = GPT-5.6 Sol / GPT-5.5 / GPT-5.4, value = GPT-5.6 Terra / GPT-5.6 Luna.
  gpt-5.6 세 기종(sol/terra/luna)은 낯선 명명이라 모델 문서로 실존을 교차 확인함
- Google (출처: ai.google.dev/gemini-api/docs/pricing + /models):
  top = Gemini 3.1 Pro Preview / 3.7 Flash / 3.6 Flash,
  value = 3.5 Flash-Lite / 2.5 Flash-Lite
- **주의: 3.7/3.6 Flash 단가($0.75/$3.75)는 2026-12-31 까지의 할인가.**
  2027-01-01 부터 2배가 된다 — 그때 표를 다시 볼 것 (아래 "정해야 할 것" 참조)
- 테스트도 함께 갱신: "비워 둔다" 검사를 "5개, top 3 + value 2" 검사로 교체,
  새 단가 계산 검사 추가. 8벌 전체 통과 확인 (2026-08-15)

### 2. 화면 렌더 확인 (E1 외 전부)

브라우저로 눈으로 본 것은 **E1 뿐이다.** 나머지는 테스트만 통과한 상태다.
테스트는 DOM 을 그리지 않으므로(import·서명·이모지·style 태그 금지만 본다)
여백이 틀어졌거나 자리가 어긋난 것은 못 잡는다.

확인이 필요한 것: **P1 · P2 · W1 · D1**, 그리고 **S1 설정 팝업**.
특히 팝업이 화면 가운데에 뜨는지, 뒤의 두 판이 흐려진 채 남는지
(2026-08-17 피드백으로 왼쪽 서랍에서 가운데 팝업으로 바뀜).

P1 부터는 키 확인을 통과해야 들어가지고, 4안 생성은 모델을 실제로 부른다(비용 발생).

---

## 정해야 할 것

### 호출당 어림 비용 되살리기 (재검토 조건: P1/W1 실호출로 평균 토큰 실측 후)

E1 비용 표의 "4안 1회 / 대화 1회" 칸을 2026-08-15 디자인 피드백으로 뺐다.
어림의 근거였던 `EST` 토큰 수가 실측이 아니라 짐작이었기 때문. 화면 렌더 확인
작업(위 2번)에서 실제 호출을 돌리게 되면 그때 usage 를 받아 평균을 재고,
그 값으로 칸을 되살릴지 결정한다. 관련 코드는 e1.js 에서 제거된 상태
(EST / runCost / money — git 이력 ca4a38f 이후 참조).

### API 키 브라우저 저장을 어디서 켜게 할지 (재검토: 위 2번 렌더 확인 작업 때)

"이 브라우저에 저장" 토글을 E1 발판에서 뺐다(2026-08-15 디자인 피드백).
S1 설정 서랍에는 저장 토글이 없어서, 지금은 저장을 켤 UI 가 어디에도 없다 —
키는 탭에서만 유지된다. 과거에 저장해 둔 키를 읽는 경로(stored)는 살아 있다.
S1 서랍의 키 관리에 토글을 넣을지, 저장 기능 자체를 접을지를 S1 렌더 확인 때
같이 정한다.

### 모델 직접 입력을 되살릴지 (재검토 조건: 받아온 목록에 없는 모델을 써야 하는 일이 실제로 생기면)

E1 의 모델 고르기에서 "직접 입력" 칸을 2026-08-17 디자인 피드백으로 뺐다.
지금은 추천 카드와, "모델 선택" 버튼으로 여는 전체 목록(제공자 API 가 준
목록)에서만 고를 수 있다. 목록에 안 잡히는 모델 ID 를 손으로 넣을 길이
없어졌는데, 제공자 목록 API 가 쓸 만한 모델을 빠뜨리는 일이 실제로
관찰되면 그때 되살린다. 관련 코드는 git 이력 참조 (#custom 입력칸).

### Gemini 3.7/3.6 Flash 단가 갱신 (재검토: 2026-12 마지막 주)

지금 표에 넣은 $0.75/$3.75 는 2026-12-31 까지의 할인가다. 2027-01-01 부터
$1.50/$7.50 로 오른다. 12월 마지막 주에 공식 요금 페이지를 다시 조회해
`webapp/providers.js` 의 google 표와 note 문구를 갱신할 것.

### [완료 2026-08-04] 제품 이름 표기

화면에 `Tstory Skin Maker` 로 나간다. 사용자가 이 표기가 맞다고 확인해 주어
그대로 확정. 저장소 이름 `Ts_skin_maker` 를 따른 축약 표기다.

---

## 알고 있는 한계

이건 버그가 아니라 "이렇게 만들기로 한 것"이다. 고치려 들기 전에 이유를 볼 것.

- **`chatModels` 필터는 이름 기반이다.** `webapp/providers.js` 의 `NOT_CHAT` 정규식으로
  음성·이미지·임베딩 모델을 걸러 낸다. 새 종류가 나오면 못 걸린다.
  **빼는 목록**으로 만든 것은 의도다 — 모르는 이름은 남긴다. 반대로 만들면
  새로 나온 대화 모델이 사라져 쓸 수 없게 되고, 그쪽이 더 나쁘다
- **단가 표는 조회 시점 기준이다.** 앤트로픽은 claude-api 카탈로그(2026-06-24),
  OpenAI/Google 은 공식 요금 페이지(2026-08-15) 조회분. 가격이 바뀌면 표도 낡는다.
  E1 비용 표는 어림값이라고 화면에 적어 두었다
- **미리보기가 못 잡는 것이 있다.** 렌더러는 티스토리가 본문에 씌우는 래퍼를
  재현하지 않는다. 문단 여백 같은 문제는 실제 블로그에 올려야 드러난다.
  `webapp/harness/pitfalls.js` 가 그 목록이다

---

## src/ 스킨 쪽 미해결

webapp 과 별개로 손으로 만든 스킨에 남아 있는 것.

- **페이지 기능 미지원.** 독립 고정 문서(페이지)는 `<s_page_rep>` 치환자가 필요한데
  아직 없다. 사용자가 페이지 기능을 쓰게 되면 추가할 것
- **형광펜 방어 CSS 가 주석 상태.** `style.css` 17번 섹션. 실제 글로 에디터가
  뿜는 HTML 을 확인한 뒤 켤 것
- **이미지 지연 로딩 미구현.** 관련 스킨 옵션 변수도 함께 제거된 상태
