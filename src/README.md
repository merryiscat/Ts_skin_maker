# Code Editor Blog - 티스토리 스킨

Cursor AI 에디터 스타일의 모던한 기술 블로그 스킨입니다.

## 🎨 주요 특징

- **에디터 스타일 디자인**: Cursor AI 에디터를 모티브로 한 다크 테마
- **코드 친화적**: 개발자를 위한 최적화된 코드 블록 스타일
- **반응형 디자인**: 모바일, 태블릿, 데스크톱 완벽 지원
- **SEO 최적화**: 검색 엔진 최적화 및 소셜 미디어 공유 태그
- **성능 최적화**: 이미지 지연 로딩, 불필요한 스크립트 차단
- **비개발자 친화적**: 상세한 주석으로 쉬운 유지보수

## 📁 파일 구조

```
src/
├── index.xml           # 스킨 메타데이터 및 설정 변수
├── skin.html           # HTML 템플릿 (티스토리 치환자 포함)
├── style.css           # 커스텀 CSS 스타일
├── images/
│   └── script.js       # JavaScript 기능
└── README.md           # 이 파일
```

## 🚀 설치 방법

### 1. 파일 준비

다음 파일들을 준비합니다:
- `index.xml`
- `skin.html`
- `style.css`
- `images/script.js`

### 2. 티스토리에 업로드

1. **티스토리 관리자** 접속
2. **꾸미기 > 스킨 편집** 메뉴로 이동
3. **html 편집** 클릭
4. 각 파일 내용을 복사하여 붙여넣기:
   - `skin.html` → HTML 편집기
   - `style.css` → CSS 편집기
   - `script.js` → 파일 업로드 섹션의 `images/script.js`
5. **적용** 버튼 클릭

### 3. 설정 변경

**관리자 > 꾸미기 > 스킨 편집 > 설정**에서 다음 옵션 조정:

- **외관 설정**
  - 메인 강조 색상 (기본: #007acc)
  - 사이드바 배경 색상
  - 메인 배경 색상
  - 텍스트 색상

- **레이아웃 설정**
  - 사이드바 너비 (200px / 250px / 300px)
  - 라이트 모드 토글 버튼 표시

- **기능 설정**
  - 검색 기능 표시
  - 목차 자동 생성
  - 이미지 지연 로딩

- **폰트 설정**
  - 코드 폰트 선택 (JetBrains Mono / Fira Code / Consolas / Monaco)

## 🎯 주요 기능

### 1. 다크/라이트 모드

- 상단 우측의 **해/달 아이콘** 클릭으로 테마 전환
- 설정이 localStorage에 저장되어 다음 방문 시에도 유지됨

### 2. 모바일 사이드바

- 768px 이하에서 햄버거 메뉴로 사이드바 표시
- 배경 클릭 또는 ESC 키로 닫기

### 3. 검색 기능

- 상단 검색창에서 바로 검색 가능
- Enter 키로 검색 실행

### 4. 목차 자동 생성

- 글 상세 페이지에서 H2, H3 제목으로 목차 자동 생성
- 목차 클릭 시 해당 섹션으로 부드럽게 스크롤

### 5. 이미지 지연 로딩

- 화면에 보이는 이미지만 로딩하여 페이지 속도 향상
- `loading="lazy"` 속성 자동 처리

### 6. 불필요한 스크립트 차단

- 티스토리 자동 삽입 스크립트 중 불필요한 것 차단
- 페이지 성능 향상

## ⚙️ 커스터마이징 가이드

### 색상 변경

**방법 1: 관리자 페이지에서 변경** (권장)
- 티스토리 관리자 > 꾸미기 > 스킨 편집 > 설정

**방법 2: CSS 직접 수정**
`style.css` 파일의 `:root` 섹션에서 변경:

```css
:root {
  --color-accent: #007acc;      /* 강조 색상 */
  --color-sidebar: #252526;     /* 사이드바 배경 */
  --color-main: #1e1e1e;        /* 메인 배경 */
  --color-text: #d4d4d4;        /* 텍스트 색상 */
}
```

### 기능 끄기

`script.js` 파일의 초기화 섹션에서 해당 줄 주석 처리:

```javascript
// initThemeToggle();        // 테마 토글 끄기
// initMobileMenu();         // 모바일 메뉴 끄기
// initSearch();             // 검색 끄기
// initTableOfContents();    // 목차 끄기
// initLazyLoading();        // 지연 로딩 끄기
// initScriptBlocker();      // 스크립트 차단 끄기
```

### 폰트 변경

**방법 1: 관리자 페이지에서 선택**
- 티스토리 관리자 > 꾸미기 > 스킨 편집 > 설정 > 폰트 설정

**방법 2: HTML에서 직접 변경**
`skin.html`의 `<head>` 섹션에서 폰트 URL 변경:

```html
<link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600&display=swap" rel="stylesheet">
```

### 사이드바 위치 변경 (우측으로)

`skin.html`에서 레이아웃 순서 변경:

```html
<!-- 기존 -->
<div class="flex pt-12">
  <aside>...</aside>    <!-- 왼쪽 -->
  <main>...</main>      <!-- 오른쪽 -->
</div>

<!-- 우측 사이드바 -->
<div class="flex pt-12">
  <main>...</main>      <!-- 왼쪽 -->
  <aside class="order-last">...</aside>  <!-- 오른쪽 -->
</div>
```

## 🐛 문제 해결

### 스타일이 적용되지 않아요

1. 브라우저 캐시 삭제 (Ctrl + Shift + R)
2. `style.css` 파일이 올바르게 업로드되었는지 확인
3. 티스토리 관리자에서 "변경사항 적용" 클릭

### 모바일에서 사이드바가 안 보여요

- 이것은 정상입니다. 모바일에서는 햄버거 메뉴 버튼을 클릭하세요.

### 검색이 작동하지 않아요

1. `index.xml`에서 `enableSearch`가 `true`인지 확인
2. `script.js`가 올바르게 로드되었는지 확인 (브라우저 콘솔)

### 목차가 생성되지 않아요

- 글에 H2 또는 H3 제목이 2개 이상 있어야 목차가 표시됩니다.
- `index.xml`에서 `enableTOC`가 `true`인지 확인하세요.

### JavaScript 에러가 발생해요

1. 브라우저 콘솔(F12) 확인
2. `script.js` 파일이 `images/` 폴더에 올바르게 업로드되었는지 확인
3. 파일 경로 확인: `./images/script.js`

## 📝 비개발자를 위한 수정 가이드

### 블로그 제목 변경

- 티스토리 관리자 > 블로그 > 정보 > 블로그 제목

### 프로필 이미지 변경

- 티스토리 관리자 > 블로그 > 정보 > 프로필 이미지

### 카테고리 추가/삭제

- 티스토리 관리자 > 글 > 카테고리 관리

### 색상만 바꾸고 싶어요

- 티스토리 관리자 > 꾸미기 > 스킨 편집 > 설정
- "외관 설정" 섹션에서 원하는 색상 클릭 후 선택

### 사이드바 너비 조정

- 티스토리 관리자 > 꾸미기 > 스킨 편집 > 설정
- "레이아웃 설정" > "사이드바 너비" 선택

## 🔧 고급 설정

### 구문 강조 (Syntax Highlighting) 추가

`skin.html`의 `<head>` 섹션에 Prism.js 추가:

```html
<!-- Prism.js CSS -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet">

<!-- Prism.js JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-javascript.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-python.min.js"></script>
```

### 코드 복사 버튼 추가

`skin.html`의 코드 블록 위에 버튼 추가:

```html
<button onclick="copyCode(this)" class="px-3 py-1 bg-gray-700 hover:bg-accent text-sm rounded">
  복사
</button>
<pre><code>코드 내용</code></pre>
```

### Google Analytics 추가

`skin.html`의 `</head>` 바로 위에 추가:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_TRACKING_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_TRACKING_ID');
</script>
```

### 파비콘 (Favicon) 추가

`skin.html`의 `<head>` 섹션에 추가:

```html
<link rel="icon" type="image/png" sizes="32x32" href="URL_TO_FAVICON">
```

## 📊 성능 최적화 팁

### 1. 이미지 최적화

- 이미지 크기 조정 (최대 1200px 너비 권장)
- WebP 형식 사용 (JPEG 대비 30% 작음)
- 압축 도구 사용: [TinyPNG](https://tinypng.com/)

### 2. CDN 활용

- Tailwind CSS는 CDN을 사용하여 빠른 로딩
- 폰트도 Google Fonts CDN 사용

### 3. 스크립트 최적화

- `defer` 속성으로 스크립트를 HTML 파싱 후 로드
- 불필요한 스크립트는 `initScriptBlocker()`로 차단

### 4. 캐시 활용

- 브라우저 캐시 활용으로 재방문 속도 향상
- 티스토리 CDN 자동 적용

## 🎨 디자인 철학

이 스킨은 **Cursor AI 에디터**의 디자인을 모티브로 합니다:

- **파일 트리**: 사이드바를 파일 탐색기처럼 디자인
- **에디터 뷰**: 본문을 코드 에디터처럼 표시
- **터미널 느낌**: 검색창에 프롬프트(`>`) 스타일
- **다크 테마**: 개발자에게 친숙한 어두운 배경

## 📄 라이선스

MIT License

자유롭게 사용, 수정, 배포할 수 있습니다.

## 🤝 기여 및 문의

- 버그 리포트: [GitHub Issues]
- 기능 제안: [GitHub Discussions]
- 이메일: your.email@example.com

## 📚 참고 자료

- [티스토리 스킨 가이드](https://tistory.github.io/document-tistory-skin/)
- [Tailwind CSS 문서](https://tailwindcss.com/docs)
- [MDN Web Docs](https://developer.mozilla.org/)

---

**즐거운 블로깅 되세요! 🚀**
